<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="152dp"
        android:layout_alignParentStart="true"
        android:layout_marginStart="0dp"
        android:scaleType="centerCrop"
        android:src="@drawable/home_bg" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="1100dp"
        android:layout_marginTop="123dp"
        android:background="@drawable/rounded_bg_home"
        android:orientation="vertical"
        android:padding="10dp">

        <ImageView
            android:id="@+id/logoImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:src="@drawable/logo"
            android:layout_marginBottom="20dp"/>

        <Button
            android:id="@+id/btnDetectWeed"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_margin="20dp"
            android:backgroundTint="@color/logo_green"
            android:drawableLeft="@drawable/ic_weed"
            android:text="Weed Detection"
            android:textSize="25sp" />

        <Button
            android:id="@+id/btnSettings"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_margin="20dp"
            android:backgroundTint="@color/logo_green"
            android:drawableLeft="@drawable/ic_settings1"
            android:text="Settings"
            android:textSize="25sp"/>

        <Button
            android:id="@+id/btnLogout"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_margin="20dp"
            android:backgroundTint="@color/logo_green"
            android:drawableLeft="@drawable/ic_logout"
            android:text="Logout"
            android:paddingLeft="45dp"
            android:textSize="25sp"/>
    </LinearLayout>
</RelativeLayout>
